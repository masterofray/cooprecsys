# Generated for Python >= 3.8
# Core Data Science (Sudah ada di Colab, tapi versi ini menjamin kompatibilitas)
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
duckdb>=0.8
scikit-learn>=1.3.0

# Library Boosting untuk LTR
lightgbm>=4.0.0
xgboost>=2.0.0
catboost>=1.2.0

# Hyperparameter Optimization & Evaluation
optuna>=3.5.0
shap>=0.44.0

# Support Libraries
pyarrow  # Penting untuk handling dataset besar (seperti Instacart) di Colab
tqdm>=4.60     # Progress bar untuk training loop

# Parallel Python utilities
joblib>=1.1

# Cython (build-time and runtime pxd cimport)
cython>=0.29

# Visualisation (optional – used if you generate evaluation plots)
seaborn>=0.12
